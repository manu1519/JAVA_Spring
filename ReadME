Solo para el archivo llamado 'todo'

En vista que el material contiene una librería que se descarga por medio de un entorno virtual (venv)

se subieron los archivos que se ocuparon para realizar el proyecto, no obstante para poder observarlo en su

totalidad, se sugiere descargar las librerías de Flask, entre otras.

--------------------------------------------------------------------------------------------------------------------
This is only for file called 'todo'

In spite of material from this content it includes a library needed to download by a virtual environ (venv) 

it is uploaded all archives that were used to make the project, nevertheless to observe all functions

it sugest to download all libraries from Flask and so on.
